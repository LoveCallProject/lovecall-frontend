module.exports = {
  // 远程音乐文件 URL 前缀, 一定要以斜线结束
  // URL prefix for remote music files, MUST end with a slash
  remoteMusicPrefix: '/music/',
  // 远程专辑封面 URL 前缀, 也要以斜线结束
  // URL prefix for remote cover art files, also MUST end with a slash
  remoteCoverArtPrefix: 'music/cover/',
};
